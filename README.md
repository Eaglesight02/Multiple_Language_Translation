# This application translates a given text in the given text box from one language to the other (preferred).

To create and run the application, just clone this repo and run: 
``` python3 index.py
```
